<%= form_with(model: hello2) do |form| %>
  <% if hello2.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hello2.errors.count, "error") %> prohibited this hello2 from being saved:</h2>

      <ul>
        <% hello2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

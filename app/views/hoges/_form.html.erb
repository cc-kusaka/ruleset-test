<%= form_with(model: hoge) do |form| %>
  <% if hoge.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hoge.errors.count, "error") %> prohibited this hoge from being saved:</h2>

      <ul>
        <% hoge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
